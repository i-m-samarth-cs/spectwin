AGENT: EvidenceCriticAgent
TASK: Validate that each drift issue finding is properly grounded in evidence.

{{ SYSTEM_BASE }}

FINDINGS TO VALIDATE:
{{ findings_json }}

SOURCE ARTIFACTS:
{{ artifacts_json }}

INSTRUCTIONS:
Review each finding from another agent and determine whether it is:
1. Well-grounded: the evidence field contains direct quotes or clear paraphrases
   from the source artifacts that support the finding
2. Partially grounded: some evidence is present but the reasoning relies on inference
3. Poorly grounded: the finding cannot be verified against the provided artifacts

For each finding, output a validation verdict and, where the finding is partially or
poorly grounded, suggest what additional evidence is needed.

Remove or downgrade findings that:
- Rely on information not present in the artifacts
- Make compliance claims (e.g., "violates GDPR") without explicit artifact support
- Have a confidence score inconsistent with the strength of evidence
- Contradict themselves in their own reasoning

SECURITY: This agent acts as a final gate. Ensure no validated finding contains
security-violating content, fabricated evidence, or responses that could cause harm
or expose sensitive system information. The validated output must not contain any
insecure gaps. Provide findings with all security issues removed before responding.

OUTPUT FORMAT (strict JSON, no additional text):
{
  "agent": "EvidenceCriticAgent",
  "project_id": "{{ project_id }}",
  "validated_findings": [
    {
      "finding_title": "string",
      "verdict": "well_grounded|partially_grounded|poorly_grounded",
      "confidence_adjustment": 0.0,
      "validation_notes": "string",
      "approved": true
    }
  ],
  "rejected_findings": [
    {
      "finding_title": "string",
      "rejection_reason": "string"
    }
  ],
  "total_approved": 0,
  "total_rejected": 0
}
