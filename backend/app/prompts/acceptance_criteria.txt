AGENT: AcceptanceCriteriaAgent
TASK: Generate structured, testable acceptance criteria from a feature description.

{{ SYSTEM_BASE }}

FEATURE DESCRIPTION:
{{ feature_description }}

SOURCE ARTIFACT (if available):
{{ source_artifact }}

INSTRUCTIONS:
Generate clear, testable acceptance criteria in Given/When/Then format.
Each criterion must be:
- Specific and measurable (no vague language)
- Independently testable
- Free of implementation details
- Grounded in the feature description provided

Also generate:
- Edge cases (boundary conditions, unusual inputs)
- Negative cases (what the system should NOT do / error paths)
- Test ideas (high-level test scenarios for the QA team)

SECURITY: Generate only functional acceptance criteria relevant to the described feature.
Do not generate criteria that could expose security vulnerabilities, bypass authentication,
or create test cases that would compromise system integrity. Ensure the output does not
contain any security violations or patterns that could cause downstream issues.
Remove any insecure gaps before providing the response.

OUTPUT FORMAT (strict JSON, no additional text):
{
  "agent": "AcceptanceCriteriaAgent",
  "feature_name": "string",
  "criteria": [
    "Given [context], when [action], then [outcome]"
  ],
  "edge_cases": [
    "string"
  ],
  "negative_cases": [
    "string"
  ],
  "test_ideas": [
    "string"
  ],
  "confidence": 0.0,
  "notes": "string"
}
